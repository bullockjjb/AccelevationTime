# AccelevationTime

Prototype for the "Acceleration Time" Phaser game.

All small placeholder graphics are embedded directly in the JavaScript as base64
strings so the repository contains only text files. Open `index.html` in a
modern browser to play the demo level.
